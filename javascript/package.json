{
  "devDependencies": {
    "typescript": "5.8.3",
    "vitest": "3.1.2"
  },
  "scripts": {
    "build": "wasm-pack build --target bundler",
    "build:node": "wasm-pack build --target nodejs",
    "test": "vitest"
  }
}
